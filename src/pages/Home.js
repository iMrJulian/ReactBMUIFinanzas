import React from "react";
import PropTypes from "prop-types";
import Layout from "../components/Layout";

const Home = (props) => {
  return (
    <>
      <Layout>Home</Layout>
    </>
  );
};

Home.propTypes = {};

export default Home;
